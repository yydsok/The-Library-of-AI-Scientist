# Git LFS configuration for large files
# Use Git LFS to store large binary files efficiently

# PDF files (research papers)
*.pdf filter=lfs diff=lfs merge=lfs -text

# PNG files (statistics and visualizations)
statistics/*.png filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
